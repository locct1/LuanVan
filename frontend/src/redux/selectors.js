import { createSelector } from '@reduxjs/toolkit';

// export const isAuthenticatedSelector = (state) => state.hostAuth.isAuthenticated;
// export const infoHostSelector = (state) => state.hostAuth.infoHost;

// export const isAuthenticatedAdminSelector = (state) => state.adminAuth.isAuthenticated;
// export const infoAdminSelector = (state) => state.adminAuth.infoAdmin;

// export const isAuthenticatedClientSelector = (state) => state.clientAuth.isAuthenticated;
// export const infoClientSelector = (state) => state.clientAuth.infoClient;

// export const infoClientBookingSelector = (state) => state.bookingNow.infoClientBooking;
// export const infoBookingDetailsSelector = (state) => state.bookingNow.bookingDetails;
// export const infoNoteBookingSelector = (state) => state.bookingNow.note;
// export const infoCheckOutSelector = (state) => state.bookingNow.isCheckOut;

// export const hostReservationListRoomsSelector = (state) => state.hostReservation.listRooms;
// export const hostReservationCheckedRoomSelector = (state) => state.hostReservation.checkRoom;
// export const hostReservationListProductsSelector = (state) => state.hostReservation.listRooms.listProducts;

// export const hostUpdateReservationSelector = (state) => state.hostUpdateReservation.reservation;
